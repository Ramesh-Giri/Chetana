<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="NavigationTheme" parent="AppTheme">
        <item name="android:layout_marginLeft">0dp</item>
    </style>

    <style name="AppToolbar" parent="Base.ThemeOverlay.AppCompat.Light">
        <item name="android:textColorPrimary">@color/toolbar_txt</item>
        <item name="android:color">@color/toolbar_txt</item>
        <item name="android:textColorSecondary">@color/toolbar_txt</item>
    </style>

</resources>
